#ifndef UTILS_H
#define UTILS_H

#include <GL/glut.h> // 包含GLUT库

// 声明一个函数，用于在指定位置绘制一个带颜色的立方体
void drawCube(float x, float y, float z, float size, float r, float g, float b);

#endif // UTILS_H